\# 架构设计规约：v1.0 温室学步期



\*\*1. 核心目标\*\*

在绝对安全、无外部干扰的物理仿真环境中，迫使网络建立 8 个 VSCMG 电机指令与航天器三轴转动方向的基础控制逻辑。



\*\*2. 环境初始化 (env.reset)\*\*

\* \*\*姿态误差\*\*：初始四元数/欧拉角随机偏差严格限制在 ±5° 以内。

\* \*\*角速度\*\*：三轴本体系角速度强制归零 (0 rad/s)。

\* \*\*飞轮状态\*\*：4 个飞轮初始转速严格锁定在 3000 rpm 标称偏置，无随机化。

\* \*\*空间扰动\*\*：强制关闭所有重力梯度、气动阻力、光压等外部力矩。



\*\*3. 动力学与动作流转 (env.step)\*\*

\* \*\*非对称动作缩放\*\*：网络输出的 \[-1, 1] 必须被精确解析。前 4 维（框架指令）最大缩放至 1 rad/s；后 4 维（飞轮指令）最大缩放至 50 rad/s^2。

\* \*\*四元数双覆盖规避\*\*：每次物理积分更新姿态后，若四元数标量部 q0 < 0，必须对整体向量取反，防止跳变。



\*\*4. 验收标准\*\*

\* 网络能迅速平滑消除 5° 偏差。

\* 飞轮转速基本维持在 3000 rpm 附近，不出现无意义饱和。

